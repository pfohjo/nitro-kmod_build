nitro-ng_kmod_build Quickstart
------------------------------

$ git submodule init
$ git submodule update
$ ./configure [--force]
$ make sync
$ make

This will generate x86/kvm-intel.ko and x86/kvm.ko.

Details
------------------------------

This is simply a fork of Jan Kiszka's repo for building external KVM modules and allows you build the nitro-ng modules without having to compile the entire kernel.

Upstream Repo
-------------------------------
git://git.kiszka.org/kvm-kmod.git
